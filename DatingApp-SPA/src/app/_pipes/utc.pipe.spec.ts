/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UtcPipe } from './utc.pipe';

describe('Pipe: Utce', () => {
  it('create an instance', () => {
    let pipe = new UtcPipe();
    expect(pipe).toBeTruthy();
  });
});
